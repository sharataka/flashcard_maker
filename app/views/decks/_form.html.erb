            <%= form_for(@deck) do |f| %> 
            <%= f.text_field :name, :class => "form-control" %>
            <br>

            <%= f.submit "Save", :class => "btn btn-large btn-success" %>


			<% end %>